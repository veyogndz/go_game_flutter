import 'package:flutter/material.dart';
class CheckWinner extends StatelessWidget {
  const CheckWinner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
